- CLI flag for `reuse annotate --import-global` uses global annotation from
  REUSE.toml to warn about global overridings and remark for existing anotations
  (when used in combination with `--skip-existing`) (#1190)
